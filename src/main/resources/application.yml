spring:
  application:
    name: geracao-faturas-cartao-credito-job

  datasource:
    sbd:
      username: username
      password: password
      jdbc-url: jdbc:mysql://localhost:3306/spring_batch?useSSL=false
    app:
      username: username
      password: password
      jdbc-url: jdbc:mysql://localhost:3306/faturas_cartao_credito?useSSL=false

  batch:
    jdbc:
      initialize-schema: always
